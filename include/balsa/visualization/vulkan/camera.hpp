#if !defined(BALSA_VISUALIZATION_VULKAN_CAMERA_HPP)
#define BALSA_VISUALIZATION_VULKAN_CAMERA_HPP

#include <memory>

namespace balsa::visualization::vulkan {
namespace objects {
    class Node;
}

class Camera {
  public:
  private:
};
}// namespace balsa::visualization::vulkan

#endif
