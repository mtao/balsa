
vis_enabled = get_option('visualization')

if vis_enabled
  #executable('vulkan_window', 'vulkan_window.cpp', dependencies: [visualization_deps])
  #
  executable('mesh_viewer', 'mesh_viewer.cpp', dependencies: [visualization_dep])
endif
